export const version = 34;
export const name = '034-progress-events';
// Progress events are stored in JSONL log files, not in PostgreSQL.
export async function up(_db) {}
export async function down(_db) {}
